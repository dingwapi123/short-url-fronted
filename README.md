# Short URL 前端

一个基于 Vue 3 + Vite + TypeScript 构建的现代化短链管理前端应用。

## 技术栈

- **框架**: Vue 3 (Composition API)
- **构建工具**: Vite
- **语言**: TypeScript
- **路由**: Vue Router
- **HTTP 客户端**: Fetch API (原生封装)
- **样式**: Tailwind CSS + DaisyUI

## 功能特性

- 🔗 **短链生成**: 支持将长链接转换为短链，可选自定义短码。
- 📋 **短链管理**: 查看所有生成的短链，支持分页浏览。
- ✏️ **编辑功能**: 修改短链标题、描述和自定义短码。
- 🗑️ **删除功能**: 安全删除不再需要的短链。
- 🚀 **客户端跳转**: 通过前端路由解析短码并跳转至原始链接。

## 快速开始

1. **安装依赖**

```bash
pnpm install
```

2. **配置环境变量**

在 `.env` 文件中配置后端 API 地址：

```env
VITE_API_BASE_URL=http://localhost:3000/v1
```

3. **启动开发服务器**

```bash
pnpm dev
```

- 开发预览地址：`http://localhost:5173`

## 页面路由

| 路径        | 说明            | 组件                      |
| ----------- | --------------- | ------------------------- |
| `/`         | 首页/短链生成页 | `src/pages/Shortener.vue` |
| `/my-urls`  | 我的短链列表    | `src/pages/MyUrls.vue`    |
| `/:urlCode` | 短链跳转页      | `src/pages/Redirect.vue`  |

## 项目结构

```
src/
├── api/             # API 接口封装
│   ├── http.ts      # Fetch API 封装
│   └── urlRecord.ts # 短链相关接口
├── assets/          # 静态资源与样式
├── components/      # 公共组件 (Toast, Dialog 等)
├── composables/     # 组合式函数 (Hooks)
├── pages/           # 页面组件
├── router/          # 路由配置
├── App.vue          # 根组件
└── main.ts          # 应用入口
```

## 常用命令

```bash
pnpm dev          # 启动开发服务器
pnpm build        # 构建生产版本
pnpm preview      # 预览构建产物
pnpm type-check   # 执行 TypeScript 类型检查
pnpm lint         # 代码规范检查与修复
```

## 部署

1. **构建项目**

```bash
pnpm build
```

2. **部署产物**

构建生成的 `dist` 目录可部署到任何静态托管服务（如 Netlify, Vercel, GitHub Pages, Nginx 等）。

**注意**: 由于项目包含客户端路由（Client-side Routing），请确保服务器配置了 SPA 重定向规则（将所有 404 请求重定向到 `index.html`）。

- **Netlify**: 项目已包含 `netlify.toml` 配置。
- **Nginx**: 需配置 `try_files $uri $uri/ /index.html;`。
